<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oacopilot.mapper.SqlHistoryMapper">

    <select id="findRecent" resultType="com.oacopilot.model.SqlHistory">
        SELECT id, prompt, sql_result, create_time
        FROM sql_history
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sql_history (prompt, sql_result, create_time)
        VALUES (#{prompt}, #{sqlResult}, #{createTime})
    </insert>

</mapper>
