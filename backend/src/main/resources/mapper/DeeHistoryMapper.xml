<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oacopilot.mapper.DeeHistoryMapper">

    <select id="findRecent" resultType="com.oacopilot.model.DeeHistory">
        SELECT id, template_type, description, result_json, create_time
        FROM dee_history
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dee_history (template_type, description, result_json, create_time)
        VALUES (#{templateType}, #{description}, #{resultJson}, #{createTime})
    </insert>

</mapper>
